# h4ki-sites
Static internal sites for Hope 4 Kids International, served by GitHub Pages from the main branch.

Publish or update a site:

    pushsite <folder-with-index.html> <slug> ["Title"]

Each site is a folder here; `sites.json` feeds the index page. Every page gets a noindex tag on push.
